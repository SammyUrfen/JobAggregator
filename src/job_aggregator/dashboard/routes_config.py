"""Config routes (Phase 8): GET /config (form) + PUT /api/config (validate + save)."""

from __future__ import annotations

# from fastapi import APIRouter
# router = APIRouter()
#   GET /config      -> config.html bound to the Pydantic Config
#   PUT /api/config  -> validate via Config, save_config(), respond "applies next run"
raise_note = "Phase 8: implement config routes"
