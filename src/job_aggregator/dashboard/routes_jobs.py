"""Jobs routes (Phase 8): GET / (server-rendered table, GET-query filters) + row actions."""

from __future__ import annotations

# from fastapi import APIRouter
# router = APIRouter()
#   GET /                      -> jobs.html with filtered rows (q/source/remote/bucket/status/...)
#   POST /api/jobs/{uid}/action -> set applied|bookmarked|hidden|notes
raise_note = "Phase 8: implement jobs routes"
