"""Phase 1 — storage.jobs_repo + runs_repo: insert, idempotent re-upsert, user-flag preservation, filtered queries, run bookkeeping.

See PLAN.md Part II (Phase 1) for the exact table-driven cases to implement.
"""

import pytest

pytestmark = pytest.mark.skip(reason="Phase 1: not yet implemented (scaffold placeholder)")


def test_placeholder() -> None:
    raise NotImplementedError("Phase 1")
